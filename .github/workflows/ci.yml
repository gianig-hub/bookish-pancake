name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  # TODO: Add lint, build, and test jobs once app shells are set up.
  # Example steps to add:
  #   - name: Install dependencies
  #     run: npm ci
  #   - name: Lint
  #     run: npm run lint
  #   - name: Type check
  #     run: npm run type-check
  #   - name: Test
  #     run: npm run test
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder — CI not yet configured
        run: echo "CI pipeline not yet configured. See NEXT-STEPS.md."
