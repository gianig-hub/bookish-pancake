# Development Dockerfile for apps/api
# Uses tsx watch for hot reload

FROM node:20-alpine

WORKDIR /app

# Install curl for health check
RUN apk add --no-cache curl

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source
COPY . .

EXPOSE 4000

CMD ["npm", "run", "dev"]
