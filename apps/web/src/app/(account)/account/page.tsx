/**
 * EK Marketplace — Account Dashboard
 * Route: /account
 * Area: Account (requires auth, any role)
 *
 * TODO: Build dashboard with:
 *   - Welcome message with user name
 *   - Quick stats (active listings, messages, alerts)
 *   - Navigation to sub-sections
 *   - Role-based feature visibility (seller features for sellers, business for businesses)
 */
export default function AccountPage() {
  return (
    <main>
      <h1>My Account</h1>
      <p>TODO: Account dashboard placeholder</p>
      {/* TODO: Show stats and quick navigation */}
    </main>
  );
}
