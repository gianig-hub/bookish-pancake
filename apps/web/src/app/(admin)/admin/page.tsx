/**
 * EK Marketplace — Admin Dashboard
 * Route: /admin
 * Area: Admin (requires auth, admin role only)
 *
 * TODO: Build admin overview with:
 *   - Platform health stats (users, listings, revenue)
 *   - Moderation queue summary
 *   - Recent signups and flags
 *   - Quick links to all admin sub-sections
 */
export default function AdminPage() {
  return (
    <main>
      <h1>Admin Dashboard</h1>
      <p>TODO: Admin overview placeholder</p>
    </main>
  );
}
