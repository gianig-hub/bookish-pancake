/**
 * EK Marketplace — Login Page
 * Route: /login
 * Area: Auth (redirect to /account if already authenticated)
 *
 * TODO: Build login form with:
 *   - Email + password fields
 *   - Remember me checkbox
 *   - Forgot password link
 *   - Register link
 *   - Social login buttons (Phase 2)
 *   - Handle LoginCredentials type from @ek/types
 */
export default function LoginPage() {
  return (
    <main>
      <h1>Sign In</h1>
      <p>TODO: Login form placeholder</p>
      {/* TODO: Implement LoginForm component */}
    </main>
  );
}
