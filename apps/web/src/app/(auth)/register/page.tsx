/**
 * EK Marketplace — Register Page
 * Route: /register
 * Area: Auth (redirect to /account if already authenticated)
 *
 * TODO: Build registration form with:
 *   - Name, email, password fields
 *   - Account type selector (personal vs business)
 *   - Role selection (buyer, private seller, trader, dealer, business)
 *   - Terms & conditions acceptance
 *   - Handle RegisterInput type from @ek/types
 */
export default function RegisterPage() {
  return (
    <main>
      <h1>Create Account</h1>
      <p>TODO: Register form placeholder</p>
      {/* TODO: Implement RegisterForm component */}
    </main>
  );
}
