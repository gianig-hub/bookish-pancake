/**
 * Worker entry point – background job processor.
 * TODO: wire up a job queue (BullMQ + Redis) once infra is ready.
 */
console.log("[worker] EK Marketplace worker starting – no jobs configured yet.");

// TODO: register job handlers (email delivery, AI processing, search indexing, etc.)
