{
  "name": "ek-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "EK Marketplace — AI-first UK vertical marketplace for HVAC, refrigeration, and smart services",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "typecheck:types": "yarn workspace @ek/types typecheck",
    "typecheck:config": "yarn workspace @ek/config typecheck",
    "typecheck:api": "yarn workspace @ek/api typecheck",
    "typecheck:web": "yarn workspace @ek/web typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
