# @ek/config

Shared configuration helpers for EK Marketplace.

## Status

Placeholder — not yet built.

## Planned Contents

- Environment variable parsing and validation (e.g., with `zod`)
- Shared constants (pagination limits, category slugs, plan names)
- Feature flag helpers

## TODO

- Add `env.ts` — validated env parsing using zod
- Add `constants.ts` — platform-wide constants
- Add `features.ts` — feature flag helpers
